import './Spinner.css';

function Spinner(){

    return (
        <div className="loading">
            <div className="spinner"/>
            <h2>Loading...</h2>
        </div>
    );
}

export default Spinner;